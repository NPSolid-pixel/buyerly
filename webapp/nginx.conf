server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location /api/ {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /health/ {
        proxy_pass http://api:8080;
        proxy_set_header Host $host;
    }

    location = /openapi.json {
        proxy_pass http://api:8080/openapi.json;
    }

    location ~ ^/(docs|redoc)($|/) {
        proxy_pass http://api:8080;
    }

    # Public documents required by Meta App Dashboard. These pages never
    # require Buyerly authentication and deliberately bypass the SPA fallback.
    location ~ ^/(privacy|terms|data-deletion)/?$ {
        add_header Cache-Control "public, max-age=300" always;
        try_files /$1.html =404;
    }

    location /static/ {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        try_files $uri =404;
    }

    location / {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        try_files $uri /index.html;
    }
}
