# Версия и сетевой режим. Deploy подставляет точный Git SHA вместо local.
APP_VERSION=local
API_HOST=0.0.0.0
API_PORT=8080
SERVE_STATIC=true

# Telegram Bot Token (от @BotFather) и необязательный чат для алертов.
BOT_TOKEN=replace-with-telegram-bot-token
ADMIN_CHAT_ID=
DEFAULT_POLL_INTERVAL_MINUTES=15

# PostgreSQL: POSTGRES_PASSWORD используется Docker Compose, DATABASE_URL —
# прямым локальным запуском. При смене пароля обновите оба значения.
POSTGRES_PASSWORD=replace-with-long-random-password
DATABASE_URL=postgresql+asyncpg://buyerly:replace-with-long-random-password@localhost:5432/buyerly

# В Docker Compose хосты сервисов называются db и redis; compose переопределяет
# DATABASE_URL автоматически. Для прямого локального Redis используйте localhost.
REDIS_URL=redis://redis:6379/0

# Публичный URL, CORS и доверенные reverse-proxy сети.
WEBAPP_URL=https://buyerly.app
CORS_ORIGINS=https://buyerly.app
TRUSTED_PROXY_CIDRS=172.16.0.0/12

# Meta OAuth / Marketing API. Секрет и ключ шифрования храните только в .env сервера.
META_GRAPH_VERSION=v26.0
META_APP_ID=
META_APP_SECRET=
META_LOGIN_CONFIG_ID=
META_OAUTH_REDIRECT_URI=https://buyerly.app/api/meta/oauth/callback
# Для ротации: сначала новый ключ, затем старый через запятую.
META_TOKEN_ENCRYPTION_KEY=

# Подписанные данные Telegram Mini App действуют 24 часа.
TELEGRAM_INIT_DATA_MAX_AGE_SECONDS=86400

# Web-сессии. В production cookie всегда передаётся только по HTTPS, а dev-auth
# должен оставаться выключенным. Для локального HTTP явно задайте secure=false.
WEB_SESSION_TTL_HOURS=24
WEB_SESSION_ROTATE_MINUTES=15
SESSION_COOKIE_SECURE=true
ENABLE_DEV_AUTH=false

# Необязательное создание первого администратора на чистой установке.
# После первого запуска эти значения можно убрать: существующие пользователи не меняются.
BOOTSTRAP_ADMIN_USERNAME=
BOOTSTRAP_ADMIN_PASSWORD=

# Транзакционные письма отправляются только через Resend REST API.
RESEND_API_KEY=
EMAIL_FROM="Buyerly <team@buyerly.app>"

# Отдельный HMAC-секрет для хранения OTP. Если пусто, используется BOT_TOKEN.
OTP_PEPPER=
