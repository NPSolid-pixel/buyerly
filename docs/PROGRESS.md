# Статус разработки и Дорожная карта (Roadmap & Progress)

В этом документе зафиксирован полный актуальный статус проекта **Buyerly**, завершенные вехи, инфраструктура и текущее состояние.

---

## 📌 Текущий статус: **Buyerly развернут на VPS-сервере в Docker, подключено 10 кабинетов, настроен канонический парсинг метрик Meta, обновлен UI и интерфейс отчетов.**

---

## 🖥 Инфраструктура и Деплой (Production VPS)
* **Сервер:** `147.45.78.208` (root, Ubuntu 26.04 LTS)
* **Контейнер:** `buyerly-bot` (Docker Compose, `restart: always`)
* **База данных:** SQLite `mediabuyer.db` смонтирована в `/opt/buyerly/data/mediabuyer.db`
* **Логи:** смонтированы в `/opt/buyerly/logs/`
* **Локальный процесс:** остановлен во избежание двойного поллинга. Бот работает автономно 24/7.

---

## 🔑 Учетные записи и роли
* **Telegram Bot:** `@buyerly_bot` (`7970458217:AAFamlyDuekEdtCOxBAwOgS-ojOwbqh2mhk`)
* **Главный администратор (Super Admin):** `@Xxq322` (`8634201356`)
* **Байер / Заказчик:** `@Nikolai_underdog` (`8948797431`)
* **Подключенные кабинеты (10 шт):**
  * `11783` (`act_1018756607700064`) — 🟢 Активен
  * `11780` (`act_1009347965254238`) — 🟢 Активен
  * `11781` (`act_1352011149770270`) — 🟢 Активен
  * `11782` (`act_2033757857229842`) — 🟢 Активен
  * `11784` (`act_1949884229056964`) — 🟢 Активен
  * `11785` (`act_906714932490620`) — 🟢 Активен
  * `11779` (`act_1485836783309489`) — 🔴 Заблокирован в Meta (Policy Ban)
  * `11786` (`act_2078379866416274`) — 🔴 Заблокирован в Meta (Policy Ban)
  * `11787` (`act_1379046641048499`) — 🔴 Заблокирован в Meta (Policy Ban)
  * `11788` (`act_1585610199765472`) — 🔴 Заблокирован в Meta (Policy Ban)

---

## 🚦 Завершенные вехи

### [x] Веха 1: Автоматизация и деплой на VPS
- [x] Отключены и удалены старые таймеры (`geo-atlas-deploy.timer`).
- [x] Dockerfile и `docker-compose.yml` оптимизированы под production.
- [x] База данных перенесена и примонтирована в Docker volume `/opt/buyerly/data/`.

### [x] Веха 2: Умный парсер кабинетов из Facebook Business Manager
- [x] Функция `parse_fb_raw_accounts` распознает сырой скопированный текст из FB BM (извлекает ID `act_...` и парсит названия).
- [x] Поддержка массового добавления пакетов аккаунтов.

### [x] Веха 3: Точный подсчет метрик без дублирования Meta API
- [x] Устранена проблема двойного подсчета `lead` + `fb_pixel_lead` и `complete_registration` + `fb_pixel_complete_registration`.
- [x] Сбор канонических уникальных событий: Лиды, Реги, Покупки (Пурчейз), Клики.
- [x] Проверено на боевых данных кабинета `11783`.

### [x] Веха 4: Режим авто-правил (Безопасный старт по умолчанию)
- [x] По умолчанию новые кабинеты подключаются в режиме `rules_enabled = False` (Только статистика).
- [x] Индивидуальный тумблер `[ 🛡 Включить авто-правила ]` / `[ 🛑 Выключить авто-правила ]` в `🏢 Мои кабинеты`.
- [x] Кнопки быстрых пресетов лимитов (`$2/$6/$6`, `$3/$8/$8`, `$5/$10/$10`) и ручной ввод.
- [x] Воркер пропускает остановку адсетов, если правила выключены на кабинете.

### [x] Веха 5: Дизайн и верстка сводок и карточек
- [x] `📊 Сводка`: чистая моноширинная таблица `<pre>`, двойные отступы, убраны CPC/CPA/адсеты/таймзоны.
- [x] `🏢 Мои кабинеты`: убрана лишняя строка «Режим: Только статистика», выводится четкий статус `🟢 Активен` / `🔴 Заблокирован в Meta`.
- [x] `💵 Расходы`: моментальный подсчет суммарного спенда за день с разбивкой по кабинетам и пометкой заблокированных.

### [x] Веха 6: CI/CD Автодеплой и скрипты обслуживания
- [x] Создан GitHub Actions workflow `.github/workflows/deploy.yml` (авто-тесты + SSH-деплой на VPS при push в `main`).
- [x] Разработан скрипт `scripts/deploy.sh` (бэкап БД, `git pull`, `docker compose up -d --build`, проверка здоровья).
- [x] Разработан скрипт `scripts/backup_db.sh` (горячий бэкап SQLite с компрессией gzip и ротацией последних 30 архивов).

### [x] Веха 7: Исчерпывающая документация проекта
- [x] Создан канонический `README.md` с бейджами, быстрым стартом, справочником команд и архитектурной схемой.
- [x] Создано руководство `docs/DEPLOYMENT.md` (DevOps, настройка VPS, GitHub Secrets, восстановление БД, troubleshooting).
- [x] Создано руководство `docs/ARCHITECTURE.md` (ERD модели данных, Mermaid диаграммы, дедупликация Meta API, FSM).

### [x] Веха 8: Техническое усиление (Hardening) Meta API и воркера
- [x] Внедрен централизованный метод `_request_with_retry` с Exponential Backoff + Jitter (2с $\rightarrow$ 4с $\rightarrow$ 8с) при 429 и 5xx ошибках.
- [x] Реализован парсинг диагностического заголовка `X-Business-Use-Case-Usage` для превентивного выявления расхода квоты $>80\%$.
- [x] Внедрен межаккаунтный случайный джиттер `0.5–1.5 сек` в цикле воркера для сглаживания сетевой нагрузки.

### [x] Веха 9: Разработка Telegram Web App (Mini App) и FastAPI Backend
- [x] Разработан REST API на базе `FastAPI` с HMAC-SHA256 валидацией `initData` Telegram Web App.
- [x] Создан адаптивный фронтенд (SPA) на чистом HTML5/CSS/JavaScript с интеграцией Telegram WebApp SDK.
- [x] Реализованы ключевые экраны: Мои кабинеты, Сводка, Добавить кабинеты, Настройки, Конструктор правил.
- [x] Интегрирован запуск FastAPI веб-сервера (Uvicorn) параллельно с Telegram ботом в едином цикле событий (`main.py`).

### [x] Веха 10: Модернизация движка правил (Rule Engine) и удаление ступенчатой логики
- [x] Полностью удалена устаревшая ступенчатая логика `$2/$6/$6` из всех слоёв (модели, движок, бот, воркер, API, Web App, тесты).
- [x] Расширен список поддерживаемых метрик в условиях: `spend`, `cpl`, `cpr`, `cpa`, `leads`, `registrations`, `purchases`, `ctr`, `cpc`.
- [x] Добавлено индивидуальное временное окно (`time_window`: `today`, `yesterday`, `last_3d`, `last_7d`) для каждого условия.
- [x] Реализована поддержка `AND` / `OR` логики объединения условий в правилах.
- [x] Добавлены новые типы действий: масштабирование бюджета `increase_budget` и `decrease_budget` с процентным шагом и максимальным дневным потолком (`budget_max_daily`).
- [x] Внедрён метод Meta API `update_adset_budget()` для прямого управления бюджетами адсетов в центах.
- [x] Обновлен интерфейс Telegram Web App: Rule Builder с новыми селекторами метрик, окон, тогглом AND/OR и блоком настройки бюджета.
- [x] Все **28 из 28** тестов (модульные, интеграционные, API, E2E) проходят успешно со 100% результатом.

---

## 🧪 Тестирование
* 28 из 28 тестов проходят успешно (`python -m unittest discover tests`).




